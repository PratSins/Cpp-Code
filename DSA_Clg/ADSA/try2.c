#include <stdio.h>
#include <stdlib.h>

// int *Add (int *a , int *b) 
// {
//     int c = (*a) + (*b) ;
//     return &c ; // error
// }
int main () 
{
    int a = 2 , b = 4 ;
    // int *ptr = Add(&a, &b);
    
    // printf (" Sum = % d \n", *ptr);
}